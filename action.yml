name: Release demo
description: Reports its source through a nested same-repository action

outputs:
  ref:
    description: Ref used to load this action
    value: ${{ steps.source.outputs.ref }}

runs:
  using: composite
  steps:
    - id: source
      uses: $/.github/actions/release-source
